<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    >
    <data>
        <variable
            name="ad"
            type="com.example.araccessories.data.dataSource.localDataSource.entities.Ad" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="342dp"
        android:background="@drawable/rounded_ad_item"

        android:layout_height="160dp">

        <TextView
            android:id="@+id/adText"

            style="@style/adText"
            android:layout_width="134dp"
            android:layout_height="111dp"
            android:layout_marginStart="16dp"
            android:text="@{ad.adName}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/imageView2"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

        </TextView>

        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="183dp"
            android:layout_height="118dp"
            android:src="@drawable/images_2"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="@string/adimage">

        </ImageView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>